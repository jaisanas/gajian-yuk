<%= form_with(model: disbursement_transaction, local: true) do |form| %>
  <% if disbursement_transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(disbursement_transaction.errors.count, "error") %> prohibited this disbursement_transaction from being saved:</h2>

      <ul>
      <% disbursement_transaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
